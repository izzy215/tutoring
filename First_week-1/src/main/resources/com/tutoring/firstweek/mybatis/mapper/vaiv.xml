<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tutoring.firstweek.mybatis.mapper.VaivMapper">



<select id="getList">


select date, title from 
(
select * from press_info left join speaker_list
on pressinfo.id = speaker_list.press_id 
union  
select * from press_info right join speaker_list
on pressinfo.id = speaker_list.press_id) 

where date > #{fromdate} 
and date<#{todate}
and name =#{name}
and party = #{party}

</select>
</mapper>